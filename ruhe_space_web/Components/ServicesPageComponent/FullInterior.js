import React from 'react';
import styles from '../../styles/FullInterior.module.scss';

const FullInterior = () => {
    return (
        <div className={`${styles.full_interior}`}>
            <div className={`${styles.inner_div} container mt-5`}>
                vishal
            </div>
        </div>
    )
}

export default FullInterior